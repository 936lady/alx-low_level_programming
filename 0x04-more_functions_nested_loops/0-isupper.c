#include  "main.h"

/**
 * _isupper - checks is a letter is uppercase
 * @c: variable to be checked
 * Return: 1 for uppercase, 0 is else
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')

		return (1);
}
