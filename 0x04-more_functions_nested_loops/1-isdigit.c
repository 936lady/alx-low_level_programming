#include "main.h"
/*
 * _isdigit - check for a digit (0 through 9).
 * @c: digit
 * Return: 1 if its from 0 to 9, 0 if else.
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
}



